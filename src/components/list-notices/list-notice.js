import React, { useState } from 'react';
import {
    View,
    Text
  } from 'react-native';

export default function ListNotice() {
  const [count, setCount] = useState(0);

  return (
    <View>
      <Text>x</Text>
    </View>
  );
}